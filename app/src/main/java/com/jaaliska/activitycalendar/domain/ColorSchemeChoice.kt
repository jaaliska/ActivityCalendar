package com.jaaliska.activitycalendar.domain

/** Colour scheme the app is painted with, in the order the settings show them. */
enum class ColorSchemeChoice {
    CRIMSON,
    BLUE,
    ORANGE,
    GREEN,
}
