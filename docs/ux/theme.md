# Тема приложения: цвет, шрифт, формы

| | |
|---|---|
| **Тип документа** | UX: тема приложения |
| **Назначение** | Зафиксировать цветовые схемы, шрифт и скругления; хранить запасные темы до появления переключателя |
| **Владелец** | Разработчик (он же продукт) |
| **Дата** | 2026-08-27 |
| **Версия** | 3 — схема считается своей математикой, Material Theme Builder не используется |
| **Связано** | [mockup-plan.md](mockup-plan.md) — как выбиралось; [information-architecture.md](information-architecture.md) — где живёт настройка |

## Решения

| Что | Решение |
|---|---|
| Цветовая схема | **Синяя**, `hue 257 / chroma 60`. Три запасные ниже |
| Шрифт | Системный **Roboto**, шкала типографики Material 3 по умолчанию |
| Скругления | Дефолтная шкала форм Material 3 |
| Material You (цвета из обоев) | **Выключен**: подстройка под обои будет спорить с фиксированными цветами активностей |
| Цвета активностей | В тему **не входят**: это цвета данных, они одинаковы во всех темах — см. таблицу в [mockup-plan.md](mockup-plan.md) |

**Правило, без которого несколько тем становятся дорогими:** в экранах не должно быть ни одного
шестнадцатеричного цвета, только роли `MaterialTheme.colorScheme`. Единственное исключение —
палитра типов активностей, она живёт отдельно.

## Цветовые схемы

Значения посчитаны в CIE LCh: ось тона там `L*`, как в материаловском HCT. Вход схемы —
**тон и насыщенность**, а не seed: колонка `Seed` осталась образцом, по которому схема
узнаётся глазами.

Формат ячейки: светлая тема / тёмная тема.

| | Малиновая | **Синяя (в работе)** | Оранжевая | Зелёная |
|---|---|---|---|---|
| Seed | `#BB576C` | `#067EB5` | `#A46947` | `#488446` |
| Тон / насыщенность | 10 / 43 | **257 / 60** | 55 / 35 | 140 / 43 |
| Фон | `#FEF8F9` / `#191112` | `#F5FAFF` / `#0E1419` | `#FFF8F5` / `#19120E` | `#F5FBF4` / `#10150F` |
| Ячейка дня | `#F1E6E7` / `#302829` | `#E3E9EF` / `#252B2F` | `#EFE6E2` / `#2F2925` | `#E4EAE3` / `#272B26` |
| Выбранный день | `#FEDADE` / `#5D3F43` | `#CCE6FE` / `#244A65` | `#F7DDD0` / `#564338` | `#D4E8D0` / `#3A4B38` |
| Primary | `#9E3D54` / `#FFB2BE` | `#076491` / `#90CDFE` | `#885030` / `#FBB792` | `#2F6B2F` / `#98D691` |

Серый фон ячеек — не отдельное решение, а производная seed: нейтральные поверхности
в Material 3 подкрашены его тоном. Поэтому подбирать оттенок выделенного дня в отрыве
от темы бессмысленно, он равен роли `secondaryContainer`.

## Как считается схема

Тон один на все палитры, различается только насыщенность:

| Палитра | Насыщенность | Что из неё |
|---|---|---|
| primary | из таблицы выше | акценты, кнопки, заголовки секций |
| secondary | треть от неё, не ниже 8 | выделенный день |
| tertiary | половина, не ниже 8 | в экранах не используется |
| neutral | 4 | фон и поверхности |
| neutral variant | 8 | контуры и приглушённый текст |

Тона ролей — стандартная таблица Material 3: в светлой теме primary 40, container 90,
surface 98, в тёмной 80 / 30 / 6. Палитра ошибки базовая материаловская, от схемы не зависит.

Считает `icon-candidates/build-color-scheme.py`, он же пишет готовый Kotlin:

```
python3 build-color-scheme.py blue > BlueColorScheme.kt
```

Запуск без аргументов сверяет все четыре схемы с таблицей выше — 32 значения. Это и есть
проверка, что схема не разъехалась с документом.

На тоне 90 насыщенность упирается в границу sRGB, поэтому в светлой теме `primaryContainer`
и `secondaryContainer` совпадают. Различать их пока негде; если понадобится — разводить тонами,
а не насыщенностью.

## Почему не Material Theme Builder

Он не переносит выбранный цвет, а пересобирает палитру схемой TonalSpot: primary режется
до насыщенности 36 при выбранных 60, а tertiary строится поворотом тона на +60°. Для синего
`257 + 60 = 317` — это пурпурный, которого в выборе не было. Проверено 27.08.2026 на seed
`#067EB5`; правила взяты из исходников `material-color-utilities`.

Поэтому tertiary у нас остаётся в тоне схемы: роль в экранах не используется, и случайный
компонент Material не должен вытащить оттуда чужой цвет.

## Смена темы

Светлая и тёмная тема следуют системной настройке телефона: своего переключателя
у приложения нет и не будет. Настраивается только цветовая схема — четырьмя кружками
в строке «Color scheme» секции «Вид» (бриф 8). До появления этой строки приложение живёт
на синей схеме.

Что надо сделать, когда до неё дойдёт дело: проверить фиксированные цвета активностей
на всех четырёх схемах в обеих темах. Сейчас они проверены только на синей, а конфликт
возможен: у оранжевой схемы primary близок к цвету бега, у зелёной — к цвету ходьбы.

---

**Definition of Done документа:** зафиксированы выбранная цветовая схема с ключевыми ролями
для светлой и тёмной темы, запасные схемы в том же формате, решения по шрифту, скруглениям
и Material You; названо правило, которое делает смену темы дешёвой, и проверка, которую
нужно провести перед включением переключателя.
